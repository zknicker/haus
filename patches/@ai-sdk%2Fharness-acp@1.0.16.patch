diff --git a/dist/bridge/index.mjs b/dist/bridge/index.mjs
index d7d322dbd6d999a40ffc8dbbf2ced4791d893de1..e86e2239a077386f86ebf541933004407215bfff 100644
--- a/dist/bridge/index.mjs
+++ b/dist/bridge/index.mjs
@@ -885,6 +885,23 @@ function captureACPStream({ stream }) {
     }
   };
 }
+function readGrokTurnUsage(usage) {
+  if (!isRecord2(usage)) return void 0;
+  for (const field of ["inputTokens", "outputTokens", "totalTokens"]) {
+    if (!Number.isSafeInteger(usage[field]) || usage[field] < 0) return void 0;
+  }
+  for (const field of ["cachedReadTokens", "cacheCreationTokens", "reasoningTokens"]) {
+    if (usage[field] != null && (!Number.isSafeInteger(usage[field]) || usage[field] < 0)) return void 0;
+  }
+  return {
+    inputTokens: usage.inputTokens,
+    outputTokens: usage.outputTokens,
+    totalTokens: usage.totalTokens,
+    cachedReadTokens: usage.cachedReadTokens,
+    cachedWriteTokens: usage.cacheCreationTokens,
+    thoughtTokens: usage.reasoningTokens
+  };
+}
 function getRawSessionUpdate({ message }) {
   const record = message;
   if (record.method !== "session/update" || !isRecord2(record.params)) {
@@ -3190,6 +3207,11 @@ async function runTurn(start, turn) {
     removeCorrelationInvocation: emitStreamEvent.removeHostToolCorrelationInvocation
   };
   hostToolRelay?.bindTurn({ turn: relayTurn });
+  let markInterjectionReady;
+  const interjectionReady = new Promise((resolve) => {
+    markInterjectionReady = resolve;
+  });
+  let acceptingInterjections = true;
   try {
     const promptMeta = createOutputSchemaPromptMeta({ start });
     void promptActiveSession({
@@ -3198,6 +3220,25 @@ async function runTurn(start, turn) {
       prompt: start.prompt,
       meta: promptMeta
     });
+    if (bridgeType === "grok-build") {
+      void (async () => {
+        for await (const message of turn.experimental_userMessages) {
+          try {
+            await interjectionReady;
+            if (!acceptingInterjections || connection == null) {
+              throw new Error("Grok turn ended before interjection.");
+            }
+            await connection.agent.request("_x.ai/interject", {
+              sessionId: activeSession.sessionId,
+              text: message.text
+            });
+            message.accept();
+          } catch (error) {
+            message.reject(error);
+          }
+        }
+      })();
+    }
     if (turn.abortSignal.aborted) {
       await cancel();
     } else {
@@ -3225,6 +3266,8 @@ async function runTurn(start, turn) {
         });
       }
       if (message.kind === "session_update") {
+        markInterjectionReady?.();
+        markInterjectionReady = void 0;
         const captured = streamCapture?.takeForUpdate({
           update: message.update
         });
@@ -3240,9 +3283,17 @@ async function runTurn(start, turn) {
       for (const rawValue of streamCapture?.drainRawValues() ?? []) {
         emitStreamEvent.raw({ rawValue });
       }
+      if (bridgeType === "grok-build" && message.kind === "stop" && message.response.usage == null) {
+        const usage = readGrokTurnUsage(message.response._meta?.usage);
+        if (usage != null) {
+          message = { ...message, response: { ...message.response, usage } };
+        }
+      }
       if (emitStreamEvent.message({ message })) return;
     }
   } finally {
+    acceptingInterjections = false;
+    markInterjectionReady?.();
     permissionController.cancelAll();
     activePermissionController = void 0;
     hostToolRelay?.unbindTurn({ turn: relayTurn });
@@ -3304,7 +3355,7 @@ async function ensureSession({
     environment: createChildEnvironment({ launchEnv })
   });
   child = spawn(
-    `${implementationDir}/node_modules/.bin/${implementation.executable}`,
+    implementation.local ? implementation.executable : `${implementationDir}/node_modules/.bin/${implementation.executable}`,
     [...implementation.args],
     {
       cwd: workDir,
@@ -3645,13 +3696,14 @@ async function readImplementationDescriptor({
   const value = await new Response(text, {
     headers: { "content-type": "application/json" }
   }).json();
-  if (!isRecord7(value) || typeof value.executable !== "string" || !Array.isArray(value.args) || !value.args.every((item) => typeof item === "string") || !Array.isArray(value.envKeys) || !value.envKeys.every((item) => typeof item === "string")) {
+  if (!isRecord7(value) || typeof value.executable !== "string" || !Array.isArray(value.args) || !value.args.every((item) => typeof item === "string") || !Array.isArray(value.envKeys) || !value.envKeys.every((item) => typeof item === "string") || typeof value.local !== "boolean") {
     throw new Error("Invalid ACP implementation descriptor.");
   }
   return {
     executable: value.executable,
     args: value.args,
-    envKeys: value.envKeys
+    envKeys: value.envKeys,
+    local: value.local
   };
 }
 function parseArgs({ args: args2 }) {
diff --git a/dist/bridge/package.json b/dist/bridge/package.json
index c320b6f1d..46cd93c1c 100644
--- a/dist/bridge/package.json
+++ b/dist/bridge/package.json
@@ -3,6 +3,7 @@
   "version": "0.0.0",
   "private": true,
   "type": "module",
+  "packageManager": "pnpm@10.32.1",
   "dependencies": {
     "@agentclientprotocol/sdk": "1.2.1",
     "@modelcontextprotocol/sdk": "1.29.0",
diff --git a/dist/index.js b/dist/index.js
index 2e37742ea..31d41a170 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -4,6 +4,10 @@ import { z as z4 } from "zod/v4";
 // src/v1/acp-v1-harness.ts
 import { createHash as createHash5, randomBytes } from "crypto";
 import { posix } from "path";
+import acpBridge from "./bridge/index.mjs" with { type: "text" };
+import acpHostToolMcp from "./bridge/host-tool-mcp.mjs" with { type: "text" };
+import acpPackageJson from "./bridge/package.json" with { type: "text" };
+import acpPnpmLock from "./bridge/pnpm-lock.yaml" with { type: "text" };
 import {
   HarnessCapabilityUnsupportedError as HarnessCapabilityUnsupportedError2,
   harnessV1DiagnosticFromBridgeFrame
@@ -11,6 +15,7 @@ import {
 import { HarnessBridgeCapabilityUnsupportedError } from "@ai-sdk/harness/bridge";
 import {
   createBridgeErrorHandler,
+  experimental_createBridgeUserMessageSubmitter,
   createBridgeStartupError,
   classifyDiskLog,
   drainBridgeProcessStream,
@@ -201,7 +206,7 @@ function validateACPV1Implementation(implementation) {
     if (source.pnpmLockYaml.length === 0) {
       throw new Error("ACP source.pnpmLockYaml must not be empty.");
     }
-  } else {
+  } else if (source.type === "npm-simple") {
     validateNpmSimpleSource({ source });
   }
   if (!EXECUTABLE_NAME_REGEXP.test(implementation.executable)) {
@@ -241,6 +246,7 @@ function createImplementationManifest({
   if (source.type === "npm-locked") {
     return source.packageJson;
   }
+  if (source.type === "local") return;
   return JSON.stringify(
     {
       name: "harness-acp-implementation",
@@ -268,7 +274,8 @@ function createImplementationDescriptor({
     {
       executable: implementation.executable,
       args: implementation.args ?? [],
-      envKeys: getImplementationEnvironmentKeys({ implementation })
+      envKeys: getImplementationEnvironmentKeys({ implementation }),
+      local: implementation.source.type === "local"
     },
     null,
     2
@@ -283,7 +290,7 @@ function createImplementationIdentity({
   permissionModeMapping
 }) {
   const { source } = implementation;
-  const sourceIdentity = source.type === "npm-locked" ? {
+  const sourceIdentity = source.type === "local" ? { type: source.type } : source.type === "npm-locked" ? {
     type: source.type,
     packageJson: source.packageJson,
     pnpmLockYaml: source.pnpmLockYaml
@@ -323,7 +330,7 @@ function createImplementationInstallCommand({
   storeDir,
   implementation
 }) {
-  return `pnpm --dir ${implementationDir} install` + (implementation.source.type === "npm-locked" ? " --frozen-lockfile" : "") + ` --prod --store-dir ${storeDir}`;
+  return `pnpm --dir ${implementationDir} install --ignore-workspace` + (implementation.source.type === "npm-locked" ? " --frozen-lockfile" : "") + ` --prod --store-dir ${storeDir}`;
 }
 function resolveImplementationEnvironment({
   implementation,
@@ -431,6 +438,7 @@ function createACPBootstrap({
       const implementationLock = getImplementationLockfile({
         implementation
       });
+      const implementationManifest = createImplementationManifest({ implementation });
       cachedBootstrap = {
         harnessId,
         bootstrapDir,
@@ -448,10 +456,10 @@ function createACPBootstrap({
             path: `${bootstrapDir}/host-tool-mcp.mjs`,
             content: hostToolMCP
           },
-          {
+          ...implementationManifest == null ? [] : [{
             path: `${bootstrapDir}/implementation/package.json`,
-            content: createImplementationManifest({ implementation })
-          },
+            content: implementationManifest
+          }],
           {
             path: `${bootstrapDir}/implementation/implementation.json`,
             content: createImplementationDescriptor({ implementation })
@@ -465,15 +473,15 @@ function createACPBootstrap({
         ],
         commands: [
           {
-            command: "pnpm install --frozen-lockfile --store-dir .pnpm-store"
+            command: "pnpm install --ignore-workspace --frozen-lockfile --store-dir .pnpm-store"
           },
-          {
+          ...implementation.source.type === "local" ? [] : [{
             command: createImplementationInstallCommand({
               implementationDir: "implementation",
               storeDir: "../.pnpm-store",
               implementation
             })
-          }
+          }]
         ]
       };
       return cachedBootstrap;
@@ -481,6 +489,13 @@ function createACPBootstrap({
   };
 }
 async function readBridgeAsset({ name }) {
+  const embeddedAssets = {
+    "host-tool-mcp.mjs": acpHostToolMcp,
+    "index.mjs": acpBridge,
+    "package.json": acpPackageJson,
+    "pnpm-lock.yaml": acpPnpmLock
+  };
+  if (embeddedAssets[name]) return embeddedAssets[name];
   const candidates = resolveBridgeAssetCandidates({
     name,
     moduleUrl: import.meta.url
@@ -1919,6 +1934,11 @@ function createSession({
     let openBlock;
     const dynamicToolCalls = /* @__PURE__ */ new Map();
     const toolCallClassificationErrors = /* @__PURE__ */ new Map();
+    const userMessageSubmitter = harnessId === "grok-build" ? experimental_createBridgeUserMessageSubmitter({
+      send: (message) => channel.send(message),
+      onResponse: (listener) => channel.on("user-message-response", listener),
+      onReconnect: (listener) => channel.onReconnect(listener)
+    }) : void 0;
     const subscriptions = [];
     const forward = (event) => {
       if (event.type === "text-start" || event.type === "reasoning-start") {
@@ -1965,6 +1985,7 @@ function createSession({
     const settle = ({ error }) => {
       if (settled) return;
       settled = true;
+      userMessageSubmitter?.close(error);
       cleanup();
       if (error === void 0) resolveDone();
       else rejectDone(error);
@@ -2077,6 +2098,11 @@ function createSession({
           isError: input.isError
         });
       },
+      ...userMessageSubmitter == null ? {} : {
+        submitUserMessage: async (text) => {
+          await userMessageSubmitter.submit(text);
+        }
+      },
       done
     };
   };
@@ -2573,4 +2599,4 @@ export {
   VERSION,
   createACP
 };
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+//# sourceMappingURL=index.js.map
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 4888c72e9..26b184a09 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -127,7 +127,10 @@ type ACPNpmLockedSource = {
     readonly packageJson: string;
     readonly pnpmLockYaml: string;
 };
-type ACPSource = ACPNpmSimpleSource | ACPNpmLockedSource;
+type ACPLocalSource = {
+    readonly type: 'local';
+};
+type ACPSource = ACPNpmSimpleSource | ACPNpmLockedSource | ACPLocalSource;
 type ACPAuthentication = {
     readonly methodId: string;
     readonly meta?: Readonly<Record<string, ACPSerializableValue>>;
