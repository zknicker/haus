diff --git a/dist/bridge/index.mjs b/dist/bridge/index.mjs
index 80e17085eaab30dc88747df1b423047165b6886a..b5697d98beb4041396ca691d5436cd572163cad6 100644
--- a/dist/bridge/index.mjs
+++ b/dist/bridge/index.mjs
@@ -930,9 +930,8 @@ function createEmitStreamEvent({
       return;
     }
     if (event.type === "error") {
-      emitError({
-        error: event.message ?? "codex error",
-        message: "codex stream error"
+      emitWarning({
+        message: event.message ?? "codex stream warning"
       });
       return;
     }
diff --git a/src/bridge/create-emit-stream-event.ts b/src/bridge/create-emit-stream-event.ts
index 337bbedc4a217dbab4542659314417ca55300508..1c46fcec378dd060d0ae7ced8b85af6c0a7ba03e 100644
--- a/src/bridge/create-emit-stream-event.ts
+++ b/src/bridge/create-emit-stream-event.ts
@@ -100,9 +100,8 @@ export function createEmitStreamEvent({
       return;
     }
     if (event.type === 'error') {
-      emitError({
-        error: event.message ?? 'codex error',
-        message: 'codex stream error',
+      emitWarning({
+        message: event.message ?? 'codex stream warning',
       });
       return;
     }
