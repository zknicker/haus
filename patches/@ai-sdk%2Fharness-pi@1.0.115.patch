diff --git a/dist/index.js b/dist/index.js
index 3ac42debf696ece30398ac1726d053cf2506c44a..cfb82d186bbe6b646c7ddba7426b1e8ddaae4234 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -520,6 +520,15 @@ async function registerCustomProviders({
     if (!baseUrl) {
       continue;
     }
+    let models;
+    const modelsJson = customEnv[`${prefix}_MODELS_JSON`];
+    if (modelsJson) {
+      try {
+        const parsed = JSON.parse(modelsJson);
+        if (Array.isArray(parsed) && parsed.length > 0) models = parsed;
+      } catch {
+      }
+    }
     await register({
       registries,
       provider,
@@ -528,7 +537,8 @@ async function registerCustomProviders({
         apiKey,
         baseUrl,
         authHeader: true,
-        ...headers ? { headers } : {}
+        ...headers ? { headers } : {},
+        ...models ? { models } : {}
       }
     });
   }
